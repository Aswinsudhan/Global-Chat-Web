{
  "buildCommand": "pnpm install --frozen-lockfile && pnpm --filter @workspace/global-connect run build",
  "outputDirectory": "artifacts/global-connect/dist/public",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
