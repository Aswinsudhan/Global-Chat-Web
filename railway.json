{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "corepack enable && pnpm install --frozen-lockfile && NODE_ENV=production pnpm --filter @workspace/global-connect run build && pnpm --filter @workspace/api-server run build"
  },
  "deploy": {
    "startCommand": "NODE_ENV=production node --enable-source-maps ./artifacts/api-server/dist/index.mjs",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
